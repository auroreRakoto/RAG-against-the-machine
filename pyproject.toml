[project]
name = "rag-against-the-machine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fire>=0.7.1",
    "pydantic>=2.13.4",
    "pytest>=8.3.5",
    "rank-bm25>=0.2.2",
    "torch>=2.12.1",
    "transformers>=5.13.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["data/raw"]
